# Personal / runtime data — never publish
guardian_state.json
.config_state
reports/
guardian.log

# Large public dataset (re-fetch with scripts/update_oui.py)
data/oui.csv

# Router / secret overrides
config_local.py
*.secret

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# runtime database (local events)
monitor.db
*.db

# nested duplicate cruft
network_monitor/
